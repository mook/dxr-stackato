# stackato.yml for indexing Komodo
tree: komodo
inherit: common.yml
mem: 1536M
# Komodo is too large to index on launch
command:
    echo "Sleeping...";
    while true;
        do sleep 3650d;
    done
